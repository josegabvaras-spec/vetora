import { PanelProveedores } from '../../features/inventario/PanelProveedores'

/**
 * «Proveedores» dentro del panel del Pet Shop.
 *
 * Envoltorio: el directorio lo comparte con `/inventario`. Ver
 * [PetshopInventarioPage](./PetshopInventarioPage.tsx).
 */
export function PetshopProveedoresPage() {
  return <PanelProveedores />
}
